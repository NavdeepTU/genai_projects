# Agent Ops Platform

Multi-agent orchestration system — a supervisor agent plans work, worker
agents execute it, and a human reviews anything irreversible before it
happens.

## Status

Scaffolding only — no code written yet. See `CLAUDE.md` for the full
build order.

## Run it locally

Not runnable yet. Setup steps will be added here once the first service
is built.

See `docs/ARCHITECTURE.md` and `docs/PROGRESS.md` for build history as
it progresses.
