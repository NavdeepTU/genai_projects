# AI Data Analyst

Text-to-SQL and insights engine — ask questions about company data in
plain English, and get back an answer, a chart, and the SQL query that
produced it.

## Status

Scaffolding only — no code written yet. See `CLAUDE.md` for the full
build order.

## Run it locally

Not runnable yet. Setup steps will be added here once the first service
is built.

See `docs/ARCHITECTURE.md` and `docs/PROGRESS.md` for build history as
it progresses.
